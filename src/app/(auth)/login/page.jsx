import LoginForm from "~/components/common/login-signup/LoginForm";

export const metadata = {
  title: "Login - Fractional Property Ownership Platform",
};

const Login = () => {
  return <LoginForm />;
};

export default Login;
